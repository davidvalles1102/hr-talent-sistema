// Layout exclusivo para la sección Reactstrap
// Importa Bootstrap CSS solo aquí para no afectar el resto del proyecto
import 'bootstrap/dist/css/bootstrap.min.css';

export default function ReactstrapLayout({ children }) {
  return <>{children}</>;
}
